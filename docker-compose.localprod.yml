services:
  marcel-caddy:
    restart: no
    ports:
      - "5443:5443"
      - "8443:8443"
    volumes:
      - ./Caddyfile.localprod:/etc/caddy/Caddyfile:ro
      
  marcel-backend:
    restart: no